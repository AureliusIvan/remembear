/**
 * @description Defines a contract for objects that have three properties: `id`,
 * `name`, and `email`.
 */
interface UserType {
  id: number
  name: string
  email: string
}

export type {UserType}